<UserControl x:Class="ChannelCurves.View.ChannelCurveBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChannelCurves.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <local:ChannelCurve DataContext="{Binding ChannelCurveViewModel0}" CurveData="{Binding PointsOnScreen}" Grid.Row="0"/>
        <local:ChannelCurve DataContext="{Binding ChannelCurveViewModel1}" CurveData="{Binding PointsOnScreen}" Grid.Row="1"/>
    </Grid>
</UserControl>
